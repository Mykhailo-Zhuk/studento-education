# AI Instructions

This folder contains the prompt files used by the AI assistant.

- `admin/` is for the logged-in admin assistant.
- `student/` is for the public student assistant that opens from a generated link.
- `admin/crud-operations.md` drives the `/crud` admin CLI shortcut.
